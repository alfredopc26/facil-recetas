import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'FacilRecetas',
  webDir: 'public',
  server: {
    androidScheme: 'https'
  }
};

export default config;
